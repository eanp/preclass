let biodata = {
    name : "budi",
    age : 20,
    address: {
        city : "jakarta",
        distric : "east"
    }
}

const {address,age,name} = biodata
const {city,distric} = address
const nama = name

// console.log(nama)

let talent = ["bayu","basri","revo"]

let  [bayu,basri,revo] = talent

// console.log(revo)